import { useState } from 'react';
import { Pause, Play } from 'lucide-react';
import { Link } from 'react-router-dom';
import { Metric, SectionHeading } from '../../components/common';
import { useApp } from '../../store/AppStore';
import { createReplayRoute, interpolateReplayLocation, MapScene } from './shared/MapScene';

export function ReplayPage() { const {telemetry}=useApp(); const [playing,setPlaying]=useState(false); const [progress,setProgress]=useState(62); const replayRoute=createReplayRoute(telemetry); const replayLocation=interpolateReplayLocation(telemetry,replayRoute,progress); return <div className="page"><SectionHeading eyebrow="FLIGHT REPLAY / STORED DATA" title="Replay a flight" copy="The map, metrics, and alert timeline are driven from the stored flight session." action={<Link to="/app/flights" className="button ghost">← All flights</Link>}/><div className="replay-panel panel"><MapScene variant="replay" progress={progress} location={replayLocation} route={replayRoute}/><div className="replay-controls"><button className="play-button" onClick={()=>setPlaying(!playing)}>{playing?<Pause size={18}/>:<Play size={18}/>}</button><div className="scrubber"><input type="range" min="0" max="100" value={progress} onChange={e=>setProgress(Number(e.target.value))}/><div><span>14:32:18</span><span>14:50:59</span></div></div><div className="speed-buttons"><button className="active">1×</button><button>2×</button><button>4×</button></div></div></div><div className="replay-metrics"><Metric label="Altitude" value={telemetry.altitude.toFixed(0)} unit="m"/><Metric label="Speed" value={telemetry.ground_speed.toFixed(0)} unit="km/h"/><Metric label="Health" value={telemetry.health_score} unit="/100"/><Metric label="Position" value="Sector 04"/></div></div>; }
